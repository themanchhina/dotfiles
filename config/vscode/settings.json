{
  // VS Code owns this file; it is tracked so its edits are versioned. Keep SSH FS
  // hosts in ~/.config/vscode-sshfs/*.json: this repo is public, and the SSH FS UI
  // writes a password into a host config if you enter one.
  "sshfs.configpaths": ["/Users/damanprc/.config/vscode-sshfs"]
}
